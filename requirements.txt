# pip install from PyPI but force rebuild
# pip install --upgrade --force-reinstall --no-cache-dir --verbose llama-cpp-python

fastapi==0.95.2
uvicorn==0.22.0
requests==2.31.0
streamlit==1.30.0
python-dotenv==1.0.0
regex==2023.12.25
